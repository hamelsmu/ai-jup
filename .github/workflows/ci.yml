name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "yarn"

      - name: Install Python dependencies
        run: |
          pip install jupyterlab>=4.0.0
          pip install -e ".[test]"

      - name: Install Node dependencies
        run: yarn install --immutable

      - name: Build extension
        run: |
          npx tsc --sourceMap
          jupyter labextension build --development True .

      - name: Run TypeScript tests
        run: yarn test

      - name: Run Python mock/unit tests
        run: |
          python -m pytest tests/test_handlers.py tests/test_parser.py \
            tests/test_api_integration.py tests/test_kernel_integration.py \
            tests/test_tool_loop.py -v

      - name: Start Jupyter server for live tests
        run: |
          jupyter lab --no-browser --ServerApp.token=debug-token &
          sleep 5
          curl -s http://localhost:8888/api/status?token=debug-token

      - name: Run live kernel tests (no API key required)
        run: |
          python -m pytest tests/test_live_kernel.py tests/test_tool_execute_handler.py -v
